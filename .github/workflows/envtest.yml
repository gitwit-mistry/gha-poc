name: ENV TEST
on: 
  workflow_dispatch:

defaults:
  run:
    shell: bash
 
jobs:
  create:
    runs-on: ubuntu-latest
    outputs:
      output1: ${{ steps.myid.outputs.timestamp}}
    steps:

      - name: env-setup
        id: myid
        run: |
          {
            echo "timestamp=3.2.1"
          } >> "$GITHUB_OUTPUT"
          
          
          
  use:
    uses: gitwit-mistry/gha-poc/.github/workflows/cp.yml@main
    with:
      timestamp: ${{ needs.create.outputs.output1 }}
    secrets: inherit
