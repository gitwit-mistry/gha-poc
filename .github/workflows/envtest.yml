name: ENV TEST
on: 
  workflow_dispatch:

defaults:
  run:
    shell: bash
 
jobs:
  create:
    runs-on: ubuntu-latest
    steps:

      - name: env-setup
        id: myid
        run: |
          {
            echo "TAG=3.2.1"
          } >> $GITHUB_OUTPUT
          
          
  use:
    uses: gitwit-mistry/gha-poc/.github/workflows/cp.yml@main
    with:
      timestamp: ${{ steps.myid.outputs.TAG }}
    secrets: inherit
